# AI 가이드

## 1. 코딩 전 생각하기 (Think Before Coding)

**추측하지 마라. 혼란을 숨기지 마라. 트레이드오프를 드러내라.**

구현하기 전에:

- 가정을 명확하게 밝혀라. 불확실하다면 질문하라.
- 여러 가지 해석이 가능하다면 이를 제시하라. 혼자 조용히 결정하지 마라.
- 더 간단한 접근 방식이 있다면 말하라. 타당하다면 이견을 제시하라.
- 불명확한 부분이 있다면 멈춰라. 무엇이 혼란스러운지 구체적으로 짚어 질문하라.

## 2. 단순함이 최우선 (Simplicity First)

**문제를 해결하는 최소한의 코드. 추측성에 기반한 코드는 배제하라.**

- 요청받은 것 이상의 기능을 추가하지 마라.
- 단 한 번만 사용하는 코드를 위한 추상화를 만들지 마라.
- 요청받지 않은 "유연성"이나 "설정 가능성(Configurability)"을 추가하지 마라.
- 발생할 리 없는 시나리오에 대한 예외 처리를 하지 마라.
- 만약 200줄로 작성한 코드를 50줄로 줄일 수 있다면, 다시 작성하라.

스스로에게 질문하라: "시니어 엔지니어가 보기에 이 코드가 지나치게 복잡하다고 할까?" 그렇다면 단순화하라.

## 3. 정밀한 변경 (Surgical Changes)

**필요한 곳만 수정하라. 오직 자신이 만든 흔적만 정리하라.**

기존 코드를 수정할 때:

- 인접한 코드, 주석, 포맷을 임의로 "개선"하지 마라.
- 고장 나지 않은 것을 리팩토링하지 마라.
- 자신이 선호하는 방식이 있더라도 기존 스타일을 일관되게 따라라.
- 관련 없는 미사용 코드(dead code)를 발견하더라도 임의로 삭제하지 말고 언급만 하라.

자신의 변경으로 인해 사용되지 않게 된 코드(orphans)가 생긴 경우:

- 자신의 변경 사항 때문에 더 이상 사용되지 않게 된 import, 변수, 함수 등은 제거하라.
- 요청받지 않은 한 기존에 이미 있던 미사용 코드는 제거하지 마라.

검증 기준: 변경된 모든 코드 라인은 사용자의 요청으로 직접 추적 가능해야 한다.

## 4. 목표 지향적 실행 (Goal-Driven Execution)

**성공 기준을 정의하라. 검증될 때까지 반복하라.**

작업을 검증 가능한 목표로 변환하라:

- "유효성 검사 추가" → "유효하지 않은 입력에 대한 테스트를 작성하고 통과시키기"
- "버그 수정" → "버그를 재현하는 테스트를 작성하고 통과시키기"
- "X 리팩토링" → "작업 전후로 테스트가 성공적으로 통과하는지 확인하기"

여러 단계로 구성된 작업의 경우 간단한 계획을 작성하라:

```
1. [단계] → 검증: [체크사항]
2. [단계] → 검증: [체크사항]
3. [단계] → 검증: [체크사항]
```

명확한 성공 기준이 있을 때 비로소 독립적인 피드백 루프를 돌 수 있다. 기준이 모호하면 ("작동하게 만들기") 끊임없이 불필요한 확인을 거쳐야 한다.

## 코드 작성 공통 규칙

**참조 검토:** 코드 생성, 수정, 삭제 시 AGENTS.md에서 적용한 규칙들 항목을 모두 출력해서 사용자에게 규칙 문서를 잘 지켰는지 알림
**규칙 학습:** 사용자가 새로운 규칙을 제시하면 즉시 AGENTS.md 규칙 섹션에 추가 · 추가 시 토큰 사용량 고려해 최대한 간결하게 작성(중복 항목엔 합치고 신규 항목만 생성)
**코드 품질:** 코드 작성 시 UX · 성능 · 보안 · 접근성 · 유지보수성 항상 고려 · 클린코드 + React식 SOLID 준수 — 단일책임(컴포넌트=UI / 훅=상태·로직 / 서비스=API조합) · ISP(props는 실제 쓰는 것만, 비대하면 분할) · DIP(컴포넌트는 구체 API 직접 호출 금지, 훅 경유) · OCP(수정보다 props·합성으로 확장, 단 변형이 실제 생길 때만) · LSP 미적용 · 간결성(최소 코드 · 불필요한 추상화·중간 변수·반복 제거 · 한눈에 의도 파악)
**레이어:** Component → Hook → Service → Api(`apis/`) 단방향 · 컴포넌트는 UI만(비즈니스 로직 금지) · 단순 CRUD는 Hook → Api(`apis/`) 직접 허용 · 비즈니스 로직 포함 시 Hook → Service → Api(`apis/`) 필수
**선언형 UI:** 컴포넌트화를 통해 선언형 UI로 구축 · 명령형 DOM 조작 금지 → 상태 기반 렌더링 · UI의 각 관심사는 독립 컴포넌트로 분리 · `renderXxx` 헬퍼 함수 금지 → JSX에 직접 인라인(`&&`·1depth 삼항), 커지면 독립 컴포넌트로 추출
**기존 자원 우선:** 구현·수정 전 `package.json`·`components/`·`hooks/`·`utils/` 확인 필수 → 활용 가능하면 반드시 재사용(동일 로직 중복 구현 금지) · 없을 때만 신규 생성
**자원 배치:** 상수·함수 등 생성/수정 시 `src/` 공통 폴더 중 적합한 곳 충분히 검토 — 순수 데이터·상수 `constants/` · 순수 로직 함수 `utils/` · 전역 상태 `stores/` · 외부 연동·헬퍼 `lib/` · 단일 API 호출 `apis/` · 단일 사용처 전용값(UI 구성 등)만 colocate · 타입·컴포넌트·훅·서비스는 각 전용 규칙 참조
**타입:** `any` 금지 → `unknown` + 타입 가드 · Props는 파일 상단 `interface [Name]Props`
**네이밍:** `camelCase` 변수·함수·폴더·타입·파일 · `PascalCase` 컴포넌트(.tsx) · `UPPER_SNAKE_CASE` 상수 · boolean 변수·Props는 `is` 접두어 필수(`isOpen` `isLoading` `isDisabled`) · API 타입은 요청 `XxxRequest`·응답 `XxxResponse` 접미어(`Dto`·`Req`·`Res` 혼용 금지)
**주석 작성:** 값/상태/ref 변수, interface 데이터 프로퍼티(string·number 등) → 선언 오른쪽 인라인(`const x = 1; // 설명`) · 함수·컴포넌트·훅·useEffect·useMemo·useCallback, interface 함수 프로퍼티(콜백·메서드) → 선언 위 JSDoc(`/** 설명 */`) · 커스텀 훅은 선언부 JSDoc + 내부 로직 주석 필수 · 코드를 생성·수정할 때 해당 주석도 함께 추가/갱신할 것
**하드코딩 금지:** 라우트·문자열·매직값(숫자 등)은 반드시 상수로 분리 · 색상·간격은 디자인 토큰 규칙 따름
**디자인 토큰:** 색상·반경은 반드시 `globals.css`에 정의된 CSS 변수 사용(shadcn 기반) — 하드코딩된 색상 값 금지 → 토큰 우선
**상태 관리:** Props 2depth 초과 → Context/상태관리로 격상
**조건부:** 3항 연산자 1depth 제한 · 복잡하면 변수 추출 · &&는 boolean 조건만(`0 &&` 금지)
**비동기:** async/await 우선(Promise chain 금지) · loading·error·empty 3상태 필수 처리
**에러 처리:** Api에서 네트워크·HTTP 에러를 도메인 에러로 변환 후 throw · Error Boundary는 페이지 단위 필수 적용
**UI 동작:** `window.alert/confirm` 금지 → Dialog/Toast 사용
**환경변수:** `process.env.*` 직접 참조 금지 → 반드시 `constants/config.ts`에서 래핑 후 사용 · `NEXT_PUBLIC_*`은 클라이언트 노출 여부 주석 명시
**Import 순서:** 외부 → 내부(`@/`) → 타입(`import type`) 각 섹션은 빈 줄로 구분
**Export:** 컴포넌트 named · 배럴 index(index.ts re-export) 금지 — 직접 경로로 import
**문법:** ES2020+ 최신 문법 우선 사용 · `for`/`for...of` 금지 → `forEach` `map` `reduce` `filter` 등 배열 내장 메서드 사용
**컴포넌트 선언 순서:** 상수 → `useRef` → `useState` → 커스텀 훅 → `useMemo`/`useCallback` → `useEffect` → 핸들러 함수 → `return JSX`

## 코드 작성 프로젝트 규칙

**[TanStack Query] 쿼리 정의:** 도메인별 `server/[feature]/[feature].queries.ts`에 키(`keys`)와 `queryOptions` · mutation 함께 정의 · `useQuery(userQueries.detail(id))` 형태로 사용 · 키 문자열 직접 사용 금지 · 계층 구조(`all→lists→detail(id)`)로 범위 무효화 가능하게 유지
**[Zustand] 셀렉터·useShallow:** `Store`는 내부에 `useShallow`가 내장되어 있도록 작성했으므로 외부에서 별도로 감쌀 필요 없음 · 셀렉터 없이 스토어 전체 구독 금지
**스타일:** 컴포넌트 파일(`.tsx`)과 반드시 동일 폴더에 `[ComponentName].module.scss` 쌍으로 생성 · 인라인 `style={{}}` 금지 → CSS Modules 사용 · 전역 스타일은 `main.scss`에만 작성
**화면 단위 컴포넌트:** 라우팅 단위 화면은 `src/screens/[name]/[Name]Screen.tsx`에 배치(예: `screens/lobby/LobbyScreen.tsx`) · 재사용 가능한 UI 조각은 `components/ui/`에 유지 · 화면과 UI 조각을 혼동하지 말 것

## 문서 구조 & 현행화 의무

작업 완료 후 변경 내용과 관련된 문서를 반드시 업데이트한다. 자동으로 업데이트하지 말고, 작업 완료 후 사용자에게 "마크다운에 반영할까요?"라고 물어본 뒤 승인받고 업데이트한다.

| 문서        | 경로                        | 업데이트 시점                               |
| ----------- | --------------------------- | ------------------------------------------- |
| 아키텍처    | `docs/arch/Architecture.md` | 폴더·레이어 구조 변경 시                    |
| 디자인      | `docs/arch/Design.md`       | 디자인 토큰·컬러·패널 규칙 변경 시          |
| 테스트      | `docs/arch/Testing.md`      | 테스트 전략·도구 변경 시                    |
| 할 일       | `docs/todo/Todos.md`        | 작업 완료·신규 할 일 추가 시                |
| 게임 콘텐츠 | `docs/content/*.md`         | 지역·NPC·몬스터·보스·직업·아이템·펫 변경 시 |
| 게임 개요   | `README.md`                 | 콘텐츠 요약 변경 시                         |
